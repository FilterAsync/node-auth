# DON'T FORGET TO ADD THIS TO THE .GITIGNORE FILE.

# MAIL

BOT_EMAIL="<bot-email>"
BOT_PASS="<bot-pass>"
SMTP_HOST="<smtp-host>"
SMTP_PORT="<smtp-port>" # INTEGER VALUE

# DATABASE + REDIS

MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.evfal.mongodb.net/<database>?retryWrites=true&w=majority"
# REPLACE <username>, <password> AS YOUR CREDENTIALS, REPLACE <database> AS YOUR CHOICE.
# OR USE THIS INSTEAD: "mongodb://localhost:27017/<database>".
REDIS_PORT="<port>" # INTEGER VALUE, DEFAULT 6379
REDIS_HOST="<host>"
REDIS_PASSWORD="<password>"

# SESSION

SESSION_SECRET="<session-secret>"
SESSION_NAME="<session-name>"

# TIMEOUTS

SESSION_TIMEOUT="<timeout>" # TIME IN MILISECONDS, RECOMMENDED: 1W (WEEK)
PASSWORD_RESET_TIMEOUT="<timeout>" # TIME IN MILISECONDS, RECOMMENDED: 1H
EMAIL_VERIFICATION_TIMEOUT="<amount>" # TIME IN MILISECONDS, RECOMMENDED: 12H/24H

# APP

APP_ORIGIN="http://localhost:<port>" # REPLACE <port> WITH YOUR PORT
APP_SECRET="<secret>" # SHOULD BE A HASH
PORT="<port>" # INTEGER VALUE

# OTHERS

BCRYPT_SALT="<salt>" # INTEGER VALUE
PASSWORD_RANDOM_BYTES="<password-random-bytes>" # INTEGER VALUE, RECOMMENDED: 40^
